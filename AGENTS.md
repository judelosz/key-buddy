# AGENTS.md — Piano Pro engineering memory

This is the **living engineering memory** for the project (build-spec §0.1). It is a map + decision log, not a duplicate of the design docs. Keep it current: at the end of any task that adds a module, changes the data model, alters a scoring/reward/algorithm parameter, or resolves an open question, update the relevant section **in the same commit**. Treat an out-of-date `AGENTS.md` as a bug.

---

## 1. Project overview

A single-user, local-first, gamified web app that teaches a complete beginner **blues, gospel, and country** piano through an interleaved, research-backed daily loop. **v1 input is a MIDI keyboard** (WEBMIDI.js) plus an on-screen **virtual keyboard**; microphone/acoustic listening is deferred (build-spec §12).

Design source-of-truth (in `docs/`). **Note the on-disk numbering — build-spec §0/§5 cite the companion docs with swapped numbers; trust the files:**

- `docs/01-curriculum-and-learning-design.md` — pedagogy, skill taxonomy, daily loop.
- `docs/02-song-library.md` — tiered, skill-tagged repertoire + song schema (build-spec calls this "03").
- `docs/03-gamification-design.md` — scoring, XP/currency, streaks, flow, roadmap, goals, guardrails (build-spec calls this "02").
- `docs/04-afk-mode-design.md` — Woodshed/AFK two-lock model.
- `docs/05-build-spec.md` — the engineering plan (source of truth for *how*).
- `docs/06-comprehensive-curriculum-plan.md` — implementation-ready 30-tier syllabus, module anatomy, exercise catalog, mastery rubric, and content contract.
- `docs/07-development-roadmap.md` — current state, phased delivery plan, exit criteria, and test/polish windows.

Scope in progress: **Phase 4 complete — in its test window.** Phases 0–4 are implemented: post-MVP shell (Missions · Free Play · AFK-coming-soon · Progress · Settings), landing onboarding, the curriculum engine (modules/lessons/exercise engine/tier gates/lesson reducer), the full authored Tier 1–5 path (16 modules, 89 lessons, 8 charts, 20 fragments, 17 theory concepts — provably completable via `walkPath.test.ts`), SongMastery foundation (levels 0–1), and the honest XP/Level meter + advancement checklist. Streaks and Riffs are deferred from the UI (data preserved). Remaining Phase-4 work: the ⏳ items in `docs/manual-test-checklist.md` (human pass, ideally with MIDI + a musical listen-through of the new arrangements). Next: Phase 5 (SessionBuilder + adaptive practice + SongMastery evidence engine); then AFK (6). Mic input stays deferred.

---

## 2. Architecture map

```
UI (React + Zustand)          src/ui/
  ↕ hooks/selectors
Core services (pure TS)       src/core/
  ContentService              content/       loads+validates skills/songs/charts JSON, queries
  types                       types.ts       the domain model (single source of truth)
  ScoringEngine               scoring/       (Chart, NotePlayed[], tempo, tier) → Attempt   [Phase 1]
  ProgressionService ★        progression/   two-lock mastery, tier, skill-gated unlocks     [Phase 3]
  RewardService ★             rewards/       XP, Riffs, streaks, variable rewards            [Phase 3]
  SpacedRepetition            srs/           ts-fsrs wrapper, due queue                       [Phase 3]
AudioService                  src/audio/     Tone.js sampler + metronome/Transport (clock)    [Phase 1]
InputService                  src/input/     source-agnostic NotePlayed stream + calibration  [Phase 1]
  providers/midiProvider                     WEBMIDI.js
  providers/virtualProvider                  on-screen / computer-key
Data layer                    src/data/      Repository interface → Dexie/IndexedDB           [Phase 3]
Content (static JSON)         src/content/   skills, songs, charts/, fragments, minigames
```

★ = modules that centrally enforce the design guardrails (§4). Data flows:
content JSON → ContentService → services; InputService → ScoringEngine → Attempt → Reward/Progression → Repository (persist) → UI via store.

**Current module status (update as built):**
- ✅ `core/types.ts`, `core/content/` (ContentService + bundled loader + validation)
- ✅ `core/scoring/` (ScoringEngine + tier timing windows) — pure, 34 unit tests
- ✅ `input/` (InputService + midi/virtual providers + calibration) — source-agnostic, calibrated stream
- ✅ `audio/audioService.ts` (Tone.Sampler sampled piano w/ synth fallback + woodblock metronome + Transport master clock, perf↔audio clock bridge)
- ✅ `ui/` debug surfaces: Input debug (note stream), Calibration, on-screen keyboard
- ✅ Phase 2 song loop: `ui/session/playSession.ts` (count-in→play→score), FallingNotes (Canvas), ChordSymbols, StaffNotation (VexFlow, secondary), SessionReport, `core/scoring/{grade,liveGrader,feedback}.ts`; charts `content/charts/*.json`
- ✅ Phase 3 progression/rewards/persistence: `core/progression/progressionService.ts` ★, `core/rewards/rewardService.ts` ★, `core/srs/fsrs.ts` (ts-fsrs), `core/session/recordAttempt.ts` (pure reducer), `data/{repository,dexieRepository}.ts`, `ui/store/gameStore.ts`, `ui/screens/Progress.tsx`. Guardrail suite `tests/unit/guardrails.test.ts`.
- ✅ Phase 4 curriculum engine: `core/curriculum/{types,validateCurriculum,tierGate,selectors}.ts` (content schemas ★, gate evaluation ★, path recommendation with spaced-review fallback), `core/exercise/{types,generators,engine}.ts` (pure exercise engine; rhythm-tap shares the scoring timing stack), `core/rewards/lessonXp.ts` ★, `core/songMastery/songMastery.ts`, `core/session/recordLesson.ts` (lesson reducer ★, checkpoint honesty). Dexie v2 (`lessonResults/lessonProgress/songMastery/chartMastery`).
- ✅ Phase 4 shell/UI: `ui/onboarding/`, `ui/missions/{Missions,ModulePath,LessonRunner,LessonResult,modeChip}`, `ui/exercises/{ExerciseShell,exerciseViews}`, `ui/session/exerciseRunner.ts`, `ui/components/{ChartPlayer(policy-driven),LevelMeter,CalibrationPanel,InputMonitorPanel}`, `ui/screens/{FreePlay,Settings,AfkComingSoon,Progress(checklist)}`.
- ✅ Phase 4 content: Tier 1–5 in `content/` — `modules.json`, `lessons/*.json`, `theoryConcepts.json`, `assessments.json`, `tierGates.json`, 8 charts, 20 fragments, 24 skills, 7 songs (incl. stretch `pinetops-boogie`). Completability proven by `tests/unit/walkPath.test.ts`.

---

## 3. Design decisions & rationale (ADR log — append-only; supersede, don't delete)

- **2026-07-22 — Level semantics: `playerLevel := learningTier`.** The user-facing Level now equals the learning tier (tier gates passed + 1, doc 07 §2.1) instead of the sqrt-XP curve; `levelForXp*` remains exported (deprecated) for the transitional header meter. Existing users keep all XP/skills/unlocks but resume at learning tier 1 (no gate evidence yet) — `normalizePlayerState` re-derives level on every load. XP fills `tierHandsXP` (the meter) but only `evaluateTierGate` advances the tier.
- **2026-07-22 — Guardrail #1 refined: Head evidence can gate but never substitute.** The tier gate requires a theory/ear checkpoint ≥80% (doc 06 §5.4) — Head evidence as a *necessary* condition. The invariant is now: Head/theory work alone can never raise learningTier, playerLevel, or currentPlayingTier (guardrail suite proves 100% Head evidence + zero Hands mastery stays tier 1).
- **2026-07-22 — New guardrail: exercises can never Hands-master.** `EXERCISE_HANDS_CAP = 0.8 < HANDS_THRESHOLD (0.85)`; only an at-tempo, un-assisted performance take reaches 1.0. Scouting/woodshed exercises contribute 0 lock progress. `trackForExerciseType` is hard-coded in core so content can't route ear/theory into Hands.
- **2026-07-22 — Checkpoint honesty: no silent assist-stripping.** An independent-mode take with a visual assist, or a performance take with any assist / slowed, records normally as an Attempt but the lesson is `passed: false`. Boss evidence reads the dedicated `chartMastery` (masteryStar) table, never star counts (stars can be earned assisted).
- **2026-07-22 — Shared gate step.** Both reducers run `applyGateAdvance` so a Free Play boss take counts toward tier advancement exactly like a Missions take (doc 06: "Free Play attempts contribute normally").

- **2026-07-22 — Scope = MVP Phases 0–3.** A runnable, honest single-song-plus-progression loop before breadth. (build-spec §8)
- **2026-07-22 — Input: source-agnostic InputService with MIDI + virtual-keyboard providers.** Jude owns a MIDI keyboard; the virtual provider makes the app runnable + Playwright-testable without hardware and is the simulated-MIDI injection point (build-spec §9). Interface reserves `source:'mic'` for the deferred acoustic path (§12) so nothing downstream changes when it lands.
- **2026-07-22 — Chart format: custom JSON (`NoteEvent[]`), tempo-independent (beats).** Simple, full control, fast to author/test; behind a loader so MusicXML import can be added later. (build-spec §10.1)
- **2026-07-22 — Content is data (JSON), auto-discovered.** `charts/*.json` loaded via Vite glob; adding content needs no code change. (build-spec §2)
- **2026-07-22 — Single soft currency "Riffs"; no leaderboards in v1; falling-notes + chord-symbols primary, staff (VexFlow) secondary; cosmetics deferred to Phase 6; desktop-first, not a PWA.** (build-spec §10 recommendations accepted)
- **2026-07-22 — Stack pinned:** React 19 + Vite 8, TypeScript 7 (strict), Zustand 5, Tailwind 3.4 (config-based), Tone.js 15, VexFlow 5, ts-fsrs 5, Dexie 4, Vitest 4 + Playwright. TS 7 removed `baseUrl` — path aliases use relative `paths` (`"@/*": ["./src/*"]`); node-side configs need `@types/node`.

---

## 4. Key invariants / guardrails (never violate — dedicated test suite in Phase 3)

From build-spec §0.1#4, doc 03 §9, doc 04 §8:

1. **`playerLevel` / `currentPlayingTier` derive ONLY from Hands progress + playing attempts.** `headTrackXP` is a separate accumulator and can never feed them. (Encoded in `PlayerState`, enforced in ProgressionService.)
2. **No buying stars/XP/skill unlocks with Riffs** — currency touches cosmetics & convenience only (streak freezes, hints, slow-downs).
3. **Song unlocks require demonstrated skill** (all required skills mastered via Hands), never currency/grind.
4. **Mastery = at-tempo, un-assisted.** Assists (`Assist[]`) and tempo are tracked on every Attempt; the mastery star requires `atTempo && assistsUsed.length === 0`.
5. **XP scales with difficulty × freshness × performance** — replaying easy/mastered content pays ~nothing.
6. **Variable rewards trigger only on good playing** — never on app-open or payment.
7. **AFK/Head progress cannot raise Player Level or playing tier; Scouting preview cap = +1 tier** (designed into Phase-3 services; AFK games built Phase 5).
8. Scoring measures **timing, not just notes**; the timing-improvement graph is the un-gameable progress measure.

---

## 4a. Design system ("Parlor Pastel", light theme)

Warm, genre-grounded light UI (tokens in `tailwind.config.ts`; globals in `src/index.css`). **Never reintroduce dark-theme classes** (`bg-ink*` as surfaces, `text-neutral-*`, `border-ink-line`).
- **Color:** `paper` (app bg), `sand` (insets/tracks), `surface` (cards), `ink`/`ink.soft` (text), `line` (borders). Accents `rose`/`amber`/`mint`/`peri`, each `-soft`/DEFAULT/`-deep`. `rose` = primary action, `amber` = hero "Play". `grade.*` stays functional (scoring) — mirror any change in `FallingNotes.tsx` GRADE_COLORS.
- **Type:** `font-display` = Fredoka (headings, numbers, buttons, chips); body = Nunito. Loaded via `@fontsource` in `main.tsx` (offline, no CDN).
- **Shape/elevation:** cards `rounded-3xl` + `shadow-soft` (hover `shadow-lift`); buttons/toggles are `rounded-full` pills; segmented controls = `bg-sand p-1` container with `bg-surface shadow-soft` active. Borders are subtle `border-line`.
- **Motion:** CSS keyframes `pop`/`fade-up`/`shimmer` (tailwind `animate-*`); tactile button press (`active:translate-y-px`), hover lift; `useCountUp` hook for reward/stat numbers. All gated by `prefers-reduced-motion` (index.css). `:focus-visible` ring for a11y.
- **Canvas** (`FallingNotes.tsx`) is hand-colored (not Tailwind): light ivory lane, rose hit line, peri/warm-neutral notes — keep in sync with the tokens if the palette changes.
- **Key alignment:** `core/pianoLayout.ts` (`keyRects`, `octaveRange`) is the SINGLE source of key geometry — both `PianoKeyboard` (CSS %) and `FallingNotes` (canvas px) use it over the same pitch range, and ChartPlayer stacks them in one full-width container so a falling note drops onto its key. If you change key layout, change it here.
- **Keyboard range policy:** one shared baseline window `DEFAULT_KEYBOARD_RANGE` (C3–B5, = the chart player's minimum) everywhere — PianoKeyboard's default, onboarding, calibration, input monitor. Content widens from there, never a fixed 88-key strip (52 white keys in the content column = unusable targets): **charts** via `displayRange(chart pitches)` (automatic), **exercises** via `ExerciseSpec.keyboardRange` (derived from the exercise's pitches, floor = default window; authorable per lesson with `generatorParams.lowPitch/highPitch`, octave-aligned). Keyboards never scroll horizontally (falling notes align by %); past 21 white keys only C landmarks keep labels; 5+-octave charts just get slimmer keys — an octave-shift control is a future option (TODO §6).

## 5. Conventions

- **TypeScript strict, no `any`.** Use `unknown` + narrowing for external data. `interface` for object shapes, `type` for unions.
- **Testable core, thin UI.** Scoring/progression/rewards/srs are pure modules independent of React; UI is a thin layer over the store.
- **Functional React components**, named prop `interface`s (no `React.FC`), immutable state updates.
- **Tailwind utility classes only.** Lucide icons. Minimal emoji, light/clean visuals (build-spec §7).
- **Path alias** `@/` → `src/`.
- **Content is JSON** under `src/content/`; validate on load (`validateContent`).
- **Tests:** Vitest unit tests in `tests/unit/**` (`*.test.ts[x]`), Playwright e2e in `tests/e2e/**`. Simulate MIDI via the virtual provider for deterministic e2e.
- **Run/build/test:** `npm run dev | test | typecheck | build | e2e`.
- **Commits:** conventional commits (`type(scope): description`); commit AGENTS.md updates alongside the change.

---

## 6. Open questions & TODOs

- **Piano sample assets.** Now a `Tone.Sampler` playing the Salamander grand piano (14 samples, every minor third A2–C6) fetched from `https://tonejs.github.io/audio/salamander/`, with the `Tone.PolySynth` synth as fallback until samples load / when offline. **Caveat: needs internet on first load** (browser-cached after). To go fully offline-first, bundle the samples locally (`public/samples/`) and repoint `baseUrl`. Organ/Rhodes/Wurli sound packs = Phase 6 cosmetics.
- **Bundle size.** Tone.js **and VexFlow** dominate the single JS chunk (~1.8 MB). Lazy-load AudioService (first audio use) and StaffNotation/VexFlow (only when the staff toggle is on), and code-split, before shipping. Fonts are separate ~20 kB woff/woff2 assets (fine). OK for local dev now.
- **Deferred: microphone / acoustic input** (build-spec §12). Not in v1. The app must clearly tell a user without a MIDI device that MIDI (or the virtual keyboard) is required. Do not assume mic support exists.
- **Chart authoring** proceeds as phases need it: Tier 1–10 starter path first (Ode to Joy, When the Saints, Amazing Grace, Oh! Susanna, 12-Bar Blues, Frankie and Johnny, C.C. Rider, Red River Valley, This Little Light of Mine), expanding toward the doc-06 30-tier syllabus.
- **Curriculum decision resolved:** use the doc-06 module anatomy (`discover → copy → recognize → vary → combine → apply → checkpoint`) and five instructional strands (technique/movement, rhythm/groove, harmony/theory, ear/musicianship, repertoire/creativity) while retaining the six existing data families.
- **Curriculum assessment decision resolved:** tier advancement requires current-tier Hands mastery, a boss-song mastery star, an ≥80% theory/ear checkpoint, and at least one delayed spaced review; Gold remains optional for advancement and still requires both locks.
- **2026-07-22 — Navigation decision resolved:** post-MVP defaults to a funneled Path experience with lightweight onboarding that launches Module 1. Free Play remains a separate, explicit area for practicing unlocked songs; it uses the same scoring/progression evidence but does not replace the guided curriculum.
- **2026-07-22 — Scale curriculum clarified:** scales are a recurring, genre-connected strand rather than a detached technical track: five-finger patterns and C/F/G pentascales first, C/F/G major scales next, then minor pentatonic/blues for blues, major pentatonic for country/gospel, and broader major/minor keys through the later tiers. Every scale is paired with an ear task and musical application.
- Notation depth, mic-scoring transparency, PWA/mobile, cosmetic art budget, social layer — revisit in later phases (build-spec §10).
- **Octave-shift control (future option):** if 5+-octave charts ever make fit-to-width keys feel cramped, add a shift/zoom affordance rather than horizontal scrolling (scrolling would break falling-note alignment). Not needed for the Tier 1–10 repertoire.
- **Tier 2–5 arrangements need a musical listen-through.** The new charts (Saints, Susanna, Mountain, Amazing Grace) are original simplified arrangements authored programmatically — contour-faithful and pedagogically structured, but they deserve an ear pass in the Phase-4 test window; melodies are trivially editable in `src/content/charts/*.json`.
- **Phase 5 stubs (deliberate, don't forget):** remediation (15)/transfer (20)/song-qualifying (25)/delayed-song-review (30) XP rows and the per-song tier-XP cap (`lessonXp.ts`); `assessment.repeatedSessions`; SongMastery levels 2–5 (need chart `sections`); review queues beyond `dueReviewSkillIds`; interval-ear beyond direction/same-different; `illustratePitches` rendering in theory quizzes.

---

## 7. Changelog (dated, human-readable)

- **2026-07-22 — Phase 5 step 2: SongMastery evidence engine (levels 2–5).** New pure `core/songMastery/sections.ts`: `sectionResults` (per-section score = blend of worst+mean bar scores from `barAccuracies` + per-bar Good-or-better timing; tunables SECTION_PASS .85 / SECTION_TIMING_MIN .7 / SECTION_BAR_FLOOR .6), derived `transitionResults` (both seam bars ≥ .75), `sliceChartSection` (rebased playable sub-chart for drills; keeps note ids). `feedback.ts` exports the shared `barOfNoteEvents` mapping. `updateSongMastery` evolved to the full doc-06 §5.2 engine: qualifying performance = full chart + zero assists + 3★ + every required section passed in that take (max 1/day, `atTempo`+`chartId` recorded); levels monotone — 2 sections learned (drills count) · 3 connected (all transitions in ONE take) · 4 ≥2 at-tempo qualifying days · 5 ≥5 performances/≥5 days/≥3 at tempo/no weak sections/delayed retrieval (≥72h gap)/≥1 transfer. Section-drill attempts (`attempt.sectionId`) move section evidence ONLY. `songMasteryDelta` (level-ups, qualifying days, delayed retrieval, new transfer → future XP rows/UI chips) + honest `detectTransfer` ('arrangement' = qualifying take on a different chart than all previous; 'reduced-guidance' = first top-quality take after non-qualifying history). Evidence keeps a legacy no-chart path (levels 0–1 only) so the Phase-4 reducers compile unchanged until step 3 threads the chart through. 13-test level-matrix suite (one-day grind caps at 3; 5 needs days+delayed+transfer); 180 unit green.
- **2026-07-22 — Phase 5 step 1: sections, spiral declarations, stretch variety (types + validation + content).** `Chart.sections` (`ChartSection {id,label,startBar,endBar,required?}`) authored on **all 8 charts** (phrase-level; 12-bar charts sectioned by form I/IV/V) with new validation: sections tile bars 0→last contiguously (`chartLastBar` exported from validate.ts), unique ids, **boss charts need ≥2 sections** (transition evidence). `Module.revisits`/`prepares` (required) authored on all 16 modules with validation (revisits look back ≤ module tier, prepares look forward ≥, every tier-2+ module revisits ≥1 skill — doc 06 §3.5 spiral made structural). `Attempt` gains `sessionId?`/`sectionId?`; `SkillProgress.handsEvidenceDates?` (distinct-day Hands evidence for `assessment.repeatedSessions`); `PlayerState.tierXpBySong` (per-song tier-XP cap substrate, reset on gate pass); `SongMastery` gains `qualifyingPerformances [{date,atTempo,attemptId}]` + `delayedRetrievalAt?` + `lastAttemptAt?` with `normalizeSongMastery` (old qualifying dates migrate as atTempo — they required the mastery star). Two new Pinetop's stretch fragments (`frag-pinetops-change-cell` tagged I-IV-V/triads, `frag-pinetops-run-cell` tagged c-major-scale) so the Phase-5 stretch selector has current-skill-sized variety. 167 unit + 10 e2e green; walkPath unaffected.
- **2026-07-22 — Header level ring links to Progress.** The top-right XP/level circle is now a button (hover lift, `aria-label` "View your progress") that opens the Progress tab from anywhere in the shell; the decorative mini-meter inside onboarding stays inert. New smoke e2e (ring click from Free Play → Progress heading); persistence spec locators tightened to `exact: true` ('Progress' nav vs the new button's accessible name). 164 unit + 10 e2e green.
- **2026-07-22 — Adaptive on-screen keyboard range.** Decision: standardize the *mechanism*, not a fixed 88-key strip (52 white keys in the app column ≈ 19px targets). One shared baseline `DEFAULT_KEYBOARD_RANGE` (C3–B5 = the chart player's existing minimum) is now PianoKeyboard's default, so exercises/onboarding/calibration/input-monitor all show the same 3-octave keyboard (was a hardcoded C4–C6). Exercises gained a per-lesson range: `ExerciseSpec.keyboardRange`, derived from the exercise's own pitches via the existing `displayRange()` (never narrower than the baseline) and authorable with `generatorParams.lowPitch/highPitch` (octave-aligned) — first authored use: `l-lh-low-notes` (C3–B4, the bass-register lesson that previously couldn't show keys below middle C). Charts keep their proven auto-widening behavior. Density guard: past 21 white keys, only C landmarks keep labels. No horizontal scrolling ever (falling-note alignment is percent-based). Verified in-browser (tap lesson shows C3–B5 with C3/C4/C5 landmarks); 164 unit + 9 e2e green.
- **2026-07-22 — Progress cleanup, tiered skill groups, note-name keys, tucked-away MIDI hint.** (1) Progress drops the "Playing tier" stat (redundant with Level for the user; `currentPlayingTier` remains internal for the scouting cap) and the skills grid is now grouped into collapsible per-tier sections sorted by tier (current learning tier open by default, each header showing x/y mastered). (2) `KeyboardHint` is a small collapsed "No MIDI keyboard?" disclosure everywhere (click to expand the Z–M/S–J/Q–U mapping); onboarding's input step opens it by default. (3) **PianoKeyboard keys are labeled with real note names** (letter per white key, octave kept on Cs as landmarks, ♯ names on black keys) instead of computer-key letters — with an honesty carve-out: note-id/build-chord lessons show labels only in guided/supported/scouting modes or when the lesson grants the `note-names` assist, so the independent note-hunt checkpoint ("no name labels") still tests the keyboard map itself. (4) Jude's local IndexedDB (localhost:5173) reset via the real `clearAll` path — stale pre-Phase-4 mastery/unlock/SongMastery state gone; clean install verified landing on onboarding. Smoke e2e updated (C4 cell vs key-label collision). 161 unit + 9 e2e green.
- **2026-07-22 — Rhythm-tap starts on the player's tap + result-button spec.** (1) Rhythm-tap lessons no longer start from a button: the player's **own first key press launches the count-in** (hands already in position; the "Start" mouse click used to eat the first clicks). The launch tap is consumed — and the engine now ignores ALL taps until the count-in anchors the beat grid, fixing a real scoring bug where a stray key press before the count-in silently counted as an extra tap against the score (regression test added). `startTaps` also guards its async init so two quick taps can't double-start. (2) LessonResult buttons per spec: **fail → "Go Back" + "Try Again" (primary)** · **pass → "Replay" + "Continue" (primary)** — pass/fail remains defined by each lesson's `passCriteria` (doc-06 thresholds), not literal 100%. Verified in-browser: tap-launch → count-in → fail state → Try Again re-arms tap-to-start. 161 unit + 9 e2e green.
- **2026-07-22 — Phase 4 test window opened (step 12).** Added `docs/manual-test-checklist.md` (living checklist per roadmap §2.1/§9). In-browser pass completed for: full onboarding walk (promise → input check with live "we heard you" → calibration → strands → XP/mastery → lands inside the first lesson), listen lesson with real audio playback + Head-XP result, note-id lesson answered on the on-screen keyboard + Hands-XP result, Missions path states (done/current/locked + mode chips), and the Progress advancement checklist with live numbers (Head pip lit from ear work). Remaining ⏳ items in the checklist need a human pass — MIDI input, calibration quality, rhythm-tap feel, checkpoint-honesty UX, the 3/4 Amazing Grace pipeline, and a musical listen-through of the new arrangements. Full suite at close: 160 unit + 9 e2e + typecheck + build green.
- **2026-07-22 — Progress tab rework (Phase 4 step 11).** The centerpiece is the doc-07 **advancement checklist**: large LevelMeter (ring = tierHandsXP/band, visually capped while gates remain) beside five evidence items (XP band with numbers · core skills x/y with the remaining skill names · boss mastery star · theory/ear checkpoint · delayed review), each incomplete item carrying a one-line "what to do" — a full ring never implies a level-up. Stats now Level (learning tier) / Playing tier / Hands XP / Head XP (**Riffs + streak removed from UI**; data persists); Hands-vs-Head explanation card; **Song Mastery card** (unlocked songs with best stars + mastery-level chip, levels 0–1 live); two-lock copy updated — Head opens through ear & theory lessons (live now, not "coming soon"). Header meter switched from the deprecated sqrt-XP curve to `levelMeter()`. Persistence e2e updated (streak stat gone from UI). 160 unit + 9 e2e green.
- **2026-07-22 — Tiers 2–5 content + review recommendation + gate tuning (Phase 4 step 10).** The full Arc I curriculum is authored and **provably completable**: 13 new modules (T2 left-hand/reading-map/Saints · T3 steps-skips/pentascales/Susanna · T4 build-triad/one-four-five/waltz-time/Mountain · T5 c-scale/chord-symbols/Amazing Grace) × 68 lessons; 5 new charts (Saints w/ 3-beat pickup + LH entering bar 8; Susanna w/ eighth-note pickups; Mountain simplified + **full** accompaniment (LH roots + RH blocked triads — the I-IV-V gate vehicle); **Amazing Grace in C, 3/4** — first non-4/4 chart); 17 new fragments (pentascales C/F/G + shift, change drills, scale + fill, song phrases, LH beds, and `frag-pinetops-groove-cell` — the first stretch Boss Challenge, `scouting`+`stretchBoss` in m5-amazing-grace); 13 new theory concepts (~70 questions incl. per-tier cross-strand checkpoint quizzes); tier gates 2–5 + assessments; stretch song `pinetops-boogie` (T24, no charts). Skills: 11 new T2–T5 skills with curriculum fields. **Design changes shaken out by the new `walkPath.test.ts`** (simulates a perfect player, one lesson/day, through the whole path): (1) `nextRecommendedLesson` now falls back to the most-overdue completed lesson as **spaced review** when all available lessons are done but the gate is closed — practicing toward the gate is the path; Missions hero shows "Spaced review" or an honest "All caught up" state (was a false "path clear"). (2) **Delayed-review evidence relaxed** (ADR): FSRS-due was too strict a proxy (all-Easy reviews push due dates months out); now a successful retrieval ≥48h after the previous review of a functional skill also counts (`DELAYED_REVIEW_MIN_GAP_MS`). (3) **Hands-XP bands calibrated** from measured natural accrual ([58,35,35,44,48] per tier for a clean run) to ~75–80%: [45,28,28,35,38] — the band is a completion floor, not a grind; delayed review is what enforces multi-day tiers. Arrangements are original simplifications — flagged for a musical listen-through in the test window. 160 unit + 9 e2e green.
- **2026-07-22 — Tier 1 vertical slice: Missions UI + first curriculum content (Phase 4 step 9).** The guided path is real: **content** — skills.json re-tiered to the doc-06 syllabus (triads/I-IV-V→T4, dom7→T7, swing→T8, Nashville→T10, minor triads→T11) + new T1 skills `ear-pitch-direction`/`rhythm-time-sig-44` with curriculum fields; 3 modules (`m1-meet-keyboard`, `m1-steady-beat`, `m1-first-melody`) × 21 lessons following discover→copy→recognize→vary→apply→checkpoint; 4 theory concepts with question pools (incl. the Tier-1 cross-strand checkpoint quiz); 3 fragments (ode phrases + pulse bed); the Tier-1 gate + assessment; Ode teaches `rhythm-time-sig-44` too (**authoring rule, now validator-enforced: every gate core skill must appear in a reachable song's taughtSkills, else Hands mastery is unreachable — exercises cap at 0.8**). **Engine** — `interval-ear` generator (up/down, same/different). **UI** — `ui/session/exerciseRunner.ts` (impure shell: calibrated input → engine, ear-prompt playback, metronome-anchored taps, `playNotesOnce` for listen lessons); `ui/exercises/{ExerciseShell,exerciseViews}.tsx` (keyboard/choice/rhythm-tap/listen surfaces); `ui/missions/{Missions,ModulePath,LessonRunner,LessonResult,modeChip}.tsx` — one dominant Continue action, due-review strip, tier-grouped Duolingo path with done/current/locked nodes + mode chips, full-screen lesson takeover (screen stays `missions`, so closing returns to path position), capability-focused result screen (XP by track, module-complete, tier-up). ChartPlayer gains the `onAttemptCaptured` lesson seam (host records via recordLesson — **no double-recording**) with mode-derived policies (guided 65% + forced falling notes … performance = pinned tempo, no assists). Onboarding now ends inside the first lesson. New `missions.spec.ts` e2e (real note-id lesson completed by clicking the on-screen keyboard; canned module completion unlocks Module 2). 159 unit + 9 e2e green.
- **2026-07-22 — Lesson reducer + store surface (Phase 4 step 8).** New pure `core/session/recordLesson.ts` (`recordLessonAttempt`): exercise lessons grade via ExerciseEngine results (XP routed by `trackForExerciseType`, locks via applyHead/applyExerciseAttempt, FSRS via `scoreToRating`, delayed-review evidence when a due functional skill passes, scouting = no locks/cards); chart lessons compose `recordChartAttempt` and layer **checkpoint honesty** (assisted/slowed checkpoint takes fail the lesson but the attempt records normally) + gate re-evaluation including the lesson's own progress. `recordChartAttempt` evolved: shared `applyGateAdvance` (tierGate.ts) re-derives `playerLevel = learningTier` and advances one tier when a gate fully passes (Free Play boss takes count); tierHandsXP accumulation, `chartMasteryStar` + `SongMastery` outputs, delayed-review detection on due functional skills at ≥2★. `gameStore` gains chartMastery/lessonProgress/songMastery state, `recordLesson`/`markSongPreviewed` actions, and selectors `moduleProgressFor`/`nextLesson` (via pure `curriculum/selectors.ts`)/`tierGateStatus`/`levelMeter` (with `requirementsRemaining`)/`songMasteryFor`/`dueReviewSkillIds`; dev seam gains `recordCannedLesson`. Guardrail #1 reworded (Head can gate, never substitute) + #1b (exercise cap) in the guardrail suite. 159 unit + 7 e2e green.
- **2026-07-22 — Progression + lesson XP + tier gates + SongMastery (Phase 4 step 7).** ProgressionService ★ gains the Head/exercise paths: `trackForExerciseType` (hard-coded in core — content can never route ear/theory into Hands; keyboard exercises note-id/build-chord/rhythm-tap/play-chart/fragment → hands, everything else → head), `applyHeadAttempt` (headLock only, monotone `headContribution`), `applyExerciseAttempt` with **`EXERCISE_HANDS_CAP = 0.8` < HANDS_THRESHOLD** (new guardrail: no amount of drilling Hands-masters a skill — only an at-tempo, un-assisted take; scouting/woodshed contribute 0). `fsrs.scoreToRating` maps exercise scores to FSRS grades. New `core/curriculum/tierGate.ts`: `evaluateTierGate` ANDs all five doc-07 conditions (core skills Hands-mastered, boss **masteryStar** evidence, checkpoints ≥ passScorePct, delayed review, tier Hands-XP band — a hard requirement) + `gateRequirementsRemaining` for the honest checklist. New `core/rewards/lessonXp.ts`: doc-07 §2.1 base values (guided 5 first-completion-only / supported 10 / independent 20 / performance 30 / scouting-stretch 8 / due-review 12) × difficulty × freshness × performance, with `repeatFactor` [1, .3, .1, 0] same-day decay; fail = 0; `awardHeadXp` touches headTrackXP only. New `core/songMastery/songMastery.ts`: levels 0–1 live (Discovered/Started), mastery-star takes accumulate one qualifying date per day for Phase 5, level never passes 1 yet. 24 new tests incl. the anti-grind gate matrix and "head-only evidence can never open a gate"; 148 unit green.
- **2026-07-22 — Exercise engine (Phase 4 step 6).** Pure `core/exercise/` layer: `generators.ts` turns a lesson's `generatorParams` into a deterministic `ExerciseSpec` (rand injected; note-id, build-chord, chord-ear, rhythm-tap, theory-quiz sampled from the concept's question pool, listen); `engine.ts` `ExerciseEngine` is a LiveGrader-style state machine — the UI runner feeds timestamped `ResponseEvent`s (`note`/`choice`/`prompt-shown`/`commit`/`watch-complete`), it grades each prompt and emits `ExerciseResult {scorePct (mean of per-prompt fractional scores), goodOrBetterPct?, timingHistogram?}`. **Rhythm-tap reuses the keyboard timing stack** (`windowsForTier` + `gradeTiming` + `matchWindowMs` + now-exported `buildHistogram`; input timestamps arrive pre-calibrated) — extra taps and missed targets count against the score; ≥70% Good-or-better passes. Chord-build accepts any inversion/octave via pitch-class sets, no extras. New `core/music.ts` helpers: `nameToPitchClass`, `parseChordSymbol` (C/Cm/C7/Cdim — early-tier vocabulary), `voiceChord`. 12 deterministic tests; 124 unit green.
- **2026-07-22 — Landing onboarding (Phase 4 step 5).** First-run gate: `PlayerState.onboardedAt` (persisted) decides onboarding vs shell after `gameStore.init()`; `ui/onboarding/{Onboarding,OnboardingSteps}.tsx` implement the doc-06 §7.0 six-step flow (promise → input setup with live "we heard you" check → inline calibration (skippable) → five strands → how Missions/XP/mastery work (explicitly: XP never levels you up alone) → launch into Missions). Replayable from Settings → Learning (replay shows a Close escape hatch, doesn't re-persist). `gameStore.completeOnboarding()` action; **`gameStore.init()` is now single-flight** (module-level promise) — the dev seam and app mount previously raced, letting a late duplicate load overwrite fresher state (caught by e2e). Seam gains `completeOnboarding()`; new `tests/e2e/helpers.ts` (`skipOnboarding`) keeps shell-focused specs direct; new `onboarding.spec.ts` covers clean-install walk-through + persistence + Settings replay. 112 unit + 7 e2e green.
- **2026-07-22 — Repository v2 (Phase 4 step 4).** Dexie schema `version(2)` adds `lessonResults` (id, lessonId, timestamp), `lessonProgress` (lessonId), `songMastery` (songId), and `chartMastery` (chartId — honest boss evidence: masteryStar boolean, because chartBest stars can be earned assisted) tables, with an `upgrade()` that normalizes existing playerState rows. Pure `normalizePlayerState(Partial<PlayerState>)` also runs on every load (belt-and-braces, unit-testable without IndexedDB): fills learningTier/tierHandsXP/tierGatePassedAt and re-derives `playerLevel = learningTier` — pre-migration users keep all XP/skills/unlocks but resume at learning tier 1 (no gate evidence yet). Repository interface + `clearAll()` extended accordingly.
- **2026-07-22 — Curriculum content schema + validation (Phase 4 step 3).** New `core/curriculum/types.ts`: content entities `Module` (ordered `lessonIds` own the path — doc-06's `Skill.lessonIds` back-reference deliberately dropped as an integrity liability), `CurriculumLesson` (mode guided/supported/independent/performance/scouting/woodshed × `exerciseType` = MiniGameType | play-chart | fragment | listen | theory-quiz, machine-checkable `passCriteria`, `stretchBoss` flag), `Assessment`, `TheoryConcept` (with question pools), `TierGate` (core skills + boss mastery star + ≥80% checkpoints + delayed review + `handsXpBand`); user-state entities `LessonResult`/`LessonProgress`/`SongMastery` (full doc-06 §5.2 shape). `Skill` gains optional curriculum fields (arc/strand/outcome/moduleId/assessment/transferTargets/commonErrors) required+validated once a Module claims the skill as core. `Attempt.refKind` adds `'fragment'`; `SkillProgress.delayedReviewPassedAt`; `PlayerState` gains `learningTier`/`tierHandsXP`/`tierGatePassedAt`/`onboardedAt` and the **guardrail #1 refinement**: *Head evidence can gate but never substitute* (a tier gate may require a theory/ear checkpoint, but Head work alone can never raise learningTier/playerLevel/currentPlayingTier). Validation split into `content/validate.ts` (base + fragments) + `curriculum/validateCurriculum.ts` (referential integrity, skill & module prereq **cycle detection**, lesson-order feasibility, complete tier gates with independent/performance checkpoint lessons, boss playability, +1 scouting cap with `stretchBoss` exemption, exercise↔ref coherence, mode/assist policy); `RawContent` + `bundled.ts` extended (lessons auto-globbed per module from `content/lessons/*.json`); ContentService gains module/lesson/assessment/concept/gate/fragment queries. 13 fixture tests in `curriculumValidation.test.ts`; 109 unit tests green.
- **2026-07-22 — ChartPlayer extraction (Phase 4 step 2).** `screens/SessionPlayer.tsx` dissolved into a reusable, policy-driven `components/ChartPlayer.tsx` (take loop: count-in → play → score → report) + `screens/FreePlay.tsx` (song picker + permissive policy — today's exact behavior). `ChartPlayerPolicy { tempo: 'choice'|number, fallingNotes: 'choice'|'on'|'off', allowWatch, allowArrangementChoice }` is how curriculum lessons will derive Guided/Supported/Independent/Performance configurations (checkpoints force assists off and tempo to target); `onRecorded` callback + `banner`/`exitLabel` slots are the lesson-runner seams. Behavior-identical extraction — no scoring/assist changes; 96 unit + 5 e2e green.
- **2026-07-22 — Post-MVP shell swap (Phase 4 step 1).** `appStore.Screen` is now `missions | free-play | afk | progress | settings` (Missions default; plus `showOnboarding`/`activeLesson` flags reserved for the onboarding + lesson-runner work). New shell in `App.tsx`: tab nav + circular `LevelMeter` (header, temporarily driven by `levelForXpBounds` until the tier-gate meter lands); Home hub dissolved into a placeholder `ui/missions/Missions.tsx` (hero + next-unlock teaser → Free Play). `Calibration`/`InputDebug` screens dissolved into embeddable `CalibrationPanel` + collapsible `InputMonitorPanel` inside the new `Settings` screen (which also adds a test-note button and a type-to-confirm full reset); `AfkComingSoon` explains Woodshed + two-lock without interactive promises. Riffs/streak chips removed from the UI per the deferral decision (persisted data untouched). e2e specs updated to the new IA; 96 unit + 5 e2e green.
- **2026-07-22 — Comprehensive curriculum plan.** Added `docs/06-comprehensive-curriculum-plan.md`, defining the 30-tier beginner-to-functional-fluency syllabus, repeatable Duolingo-style module anatomy, keyboard/AFK exercise catalog, tier gates, mastery rubric, content-authoring contract, feedback rules, research basis, and implementation order. Resolved curriculum questions in this file: retain blues as the recommended first branch, prioritize chord symbols/ear/groove while progressively adding staff reading, and use adaptive session lengths around a 20-minute default.
- **2026-07-22 — Development roadmap and MVP hardening phase.** Added `docs/07-development-roadmap.md`, formalizing the current state, Phase 3.5 user-testing window, phase-by-phase deliverables, exit criteria, manual test expectations, curriculum vertical slice, SessionBuilder, AFK, Journey, genre expansion, local-first readiness, and deferred post-v1 work. README status now identifies Phase 3.5 as current.
- **2026-07-22 — Path, onboarding, and Free Play direction.** Added the funnel/onboarding and Path-vs-Free-Play model to docs 05–07 and docs 06’s curriculum UX section. Onboarding explains modules, skills, Riffs, song unlocks, and mastery, then launches Module 1; Free Play exposes unlocked songs as open practice without bypassing progression gates.
- **2026-07-22 — Scale curriculum expansion.** Clarified scales in docs 01, 06, and 07: scale shapes must connect to sound, theory, and genre-specific riffs, fills, bass lines, melodies, or improvisation. The initial Tier 1–5 slice now includes C major scale work, with pentatonic/blues applications entering the early genre branch.
- **2026-07-22 — Curriculum strand audit.** Expanded doc 06 with continuous 1–30 ladders for technique/movement, rhythm/groove, harmony/theory, ear/musicianship, and repertoire/creativity, plus cross-strand gates at Tiers 5, 10, 15, 22, and 30. Content authoring now requires strand evidence so no tier can advance on isolated chart, quiz, or scale performance.
- **2026-07-22 — Meter curriculum clarified:** time signatures are an explicit rhythm/theory progression, beginning with felt and notated 4/4, then 2/4 and 3/4, followed by meter-aware shuffle, syncopation, groove switching, and unfamiliar-meter reading. Assessment requires identification, independent counting/tapping, and musical application.
- **2026-07-22 — Spiral curriculum clarified:** skills, scales, grooves, theory concepts, song sections, and fragments are durable review items. FSRS/session selection must resurface errors, prerequisites, changed-context maintenance, and previous-tier skills; mastery reduces frequency but never retires an item permanently.
- **2026-07-22 — Song mastery and stretch bosses clarified:** Attempt-level mastery stars are not song fluency. Durable SongMastery requires section/transition coverage, multiple qualifying sessions across days, delayed retrieval, and transfer evidence; Free Play contributes. The +10-tier stretch song appears as current-skill-sized Boss Challenges, never as an unlocked full-song test.
- **2026-07-22 — Post-MVP shell and progression direction:** onboarding becomes the landing page with a Get Started button; the app shell becomes Missions, Free Play, AFK Mode, Progress, and Settings. Missions owns learning-tier advancement. Hands XP fills the Level meter; Head/AFK XP supports knowledge and SongMastery but cannot advance the playing tier alone. Streaks and Riffs are deferred from the next UI pass.
- **2026-07-22 — Progression choices resolved:** use hybrid Hands XP (visible meter plus mandatory mastery gates), hybrid Head XP (knowledge progress and SongMastery support but no tier advancement), and no user-facing Transfer XP. Transfer is internal evidence. Use spaced evidence across separated sessions rather than a calendar/streak requirement, a difficult five-session SongMastery standard, and a visible Progress advancement checklist.
- **2026-07-22 — Multi-octave keyboard + live key highlighting.** Play view now shows a comfortable minimum window of ~3 octaves (`displayRange`, default C3–B5, widened to fit songs that reach further) instead of a tight single octave. PianoKeyboard now highlights the pressed key from the **unified input stream** (`inputService.onNote`), so MIDI, computer keys, and on-screen taps all light the key (was: only on-screen/computer-key via the component's own handler; MIDI didn't highlight).
- **2026-07-22 — Falling-notes/keyboard alignment + MIDI-on-Play.** New `core/pianoLayout.ts` (shared key geometry) so falling notes drop exactly onto their keys; PianoKeyboard rewritten to position keys by % from `keyRects`, FallingNotes places note columns from the same rects, SessionPlayer derives a shared whole-octave range (`octaveRange`) from the chart (now includes LH bass that used to fall outside the fixed C4–C6 keyboard) and stacks canvas + keyboard in one full-width container. Reusable `MidiConnectButton` added to the Play toolbar (and InputDebug) — MIDI connects alongside the always-on virtual keyboard and reaches scoring while playing.
- **2026-07-22 — Audio + input polish.** (1) Metronome is now a **woodblock** (dry pitched "tok" + band-passed pink-noise transient, accented downbeat) instead of the bass MembraneSynth. (2) Note playback uses a **sampled grand piano** (Tone.Sampler, Salamander via CDN) with the synth as fallback while loading / offline — verified all 14 samples fetch 200. (3) InputService now supports **concurrent providers**: the virtual (on-screen + computer-key) keyboard is always active, so laptop-key play works with no MIDI and continues alongside a connected device (was single-provider, replaced on MIDI select). Computer-key map lives in `virtualProvider.ts` (`COMPUTER_KEY_MAP`: Z–M white / S,D,G,H,J black / Q–U octave up); new `KeyboardHint` shown on Play + Input; InputDebug reworked to a "Connect MIDI" toggle. `appStore.providerKind` → `midiEnabled`.
- **2026-07-22 — UI restyle ("Parlor Pastel", light theme + game feel).** Visual/motion-only pass (no logic changes; all 92 unit + 4 e2e still green). Dark near-black theme → warm ivory light theme with genre-grounded pastels; Fredoka + Nunito via @fontsource; rounded-3xl cards + soft shadows + pill buttons; tactile/hover motion, star pop-in, XP/Riffs count-up (`useCountUp`), animated bars, all reduced-motion-gated. Home rebuilt as a game **hub** (hero + HUD chips + amber Play + next-unlock teaser). FallingNotes canvas re-derived for the light lane. New design-system section (§4a). Verified all screens in-browser.
- **2026-07-22 — Phase 3 (Progression + rewards + persistence) — MVP complete.** Two-lock ProgressionService ★ (Hands from playing, Head reserved for AFK; playing tier & unlocks read Hands only), RewardService ★ (XP = difficulty×freshness×performance, Riffs firewall with cosmetic/convenience-only sinks, streaks+freezes, ethical encore bonus that only fires on good playing), FSRS wrapper (ts-fsrs; star→grade, retrievability, due queue). `recordChartAttempt` pure reducer composes all three; `gameStore` persists via Repository→Dexie/IndexedDB. Progress screen (tier/level/XP/Riffs/streak, two-lock skill nodes, next-unlock endowed-progress bars); SongPicker gates locked songs with "N skills to unlock"; SessionReport shows XP/Riffs/level-up/unlocks/encore. **Dedicated guardrail suite** proves invariants 1–7. Persistence e2e (canned mastery take → unlock → reload → still unlocked → playable) via a dev-only `__pianoTest` seam (`ui/devTest.ts`, DEV-only). Decisions: HANDS_THRESHOLD 0.85 so only the mastery star Hands-masters a skill; song unlock = required skills Hands-mastered (Head/AFK & Riffs can never unlock); entry songs have empty requiredSkills (Ode teaches the basics that gate the 12-bar blues, giving a real 2-song unlock chain). 92 unit + 4 e2e tests green.
- **2026-07-22 — Phase 2 (Single playable song loop).** Full take loop: `PlaySession` orchestrates count-in → play (metronome clock) → offline scoring → report. FallingNotes Canvas visualizer synced to Transport; ChordSymbols strip (primary notation) with current-bar highlight; StaffNotation (VexFlow treble staff, secondary/best-effort, toggle off by default, defensive try/catch); LiveGrader for real-time per-note colour; SessionReport with star rating, timing histogram, weak-bar heat-map, and one actionable tip (`core/scoring/feedback.ts`, tested). Authored charts: Ode to Joy (simplified) + 12-Bar Blues in C (simplified triads / full dom7). 3 Playwright smoke e2e (nav, player render, input log). **Key fix:** metronome beat ticks now drive off a rAF loop reading the Transport clock (was `Tone.getDraw()`, which didn't reliably fire app-state callbacks and left the play phase stuck). Verified full loop in-browser incl. report. Falling-notes visualizer is treated as an `assist` (mastery star withheld while on) — honest per guardrail #4.
- **2026-07-22 — Phase 1 (Input + Audio + Scoring core).** ScoringEngine (pure `(Chart, NotePlayed[], tempo, tier) → Attempt`) with tier-interpolated timing windows, per-note grades, timing histogram, 1–3 stars + at-tempo/un-assisted mastery star — 34 deterministic unit tests. Source-agnostic InputService with WEBMIDI.js + virtual-keyboard providers (shared `performance.now()` clock), one-time latency calibration (pure median-offset computation). AudioService (Tone.js PolySynth + metronome/Transport as master clock, audio↔perf clock bridge for Phase-2 alignment). Input-debug + calibration screens + on-screen keyboard; verified in-browser (clean calibrated note stream). Decisions: timing windows interpolate linearly tier 1↔30; match window capped at ~1 beat (beyond = Miss); XP/Riffs left 0 by ScoringEngine (RewardService fills). Note: Tone.js pushes the bundle >500 kB — lazy-load later (added to TODOs).
- **2026-07-22 — Phase 0 (Scaffold).** Vite+React+TS(strict), Tailwind, Zustand, Dexie, Vitest, Playwright wired. Domain model (`core/types.ts`), ContentService with validation + Vite-glob chart loading, seed `skills.json`/`songs.json`, placeholder shell. dev/test/typecheck/build all green. git initialized.
